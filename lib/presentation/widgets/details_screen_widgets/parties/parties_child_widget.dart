import 'package:flutter/material.dart';

class PartiesChildWidget extends StatelessWidget {
  const PartiesChildWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Parties");
  }
}
