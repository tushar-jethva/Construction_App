import 'package:flutter/material.dart';

class PartiesNWidget extends StatelessWidget {
  const PartiesNWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Parties"),
    );
  }
}
