import 'package:flutter/material.dart';

class MyBuildingWidget extends StatelessWidget {
  const MyBuildingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Buildings"),
    );
  }
}